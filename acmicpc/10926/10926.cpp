// solved
#include <iostream>
#include <string>

using namespace std;

int main(void) {
    cin.sync_with_stdio(false);
    cin.tie(nullptr);
    
    string s;
    cin >> s;

    cout << s + "??!" << endl;
}
